const request = require('supertest');

const server = require('./index');

describe('Main Router', () => {
    describe('GET /', () => {
        it('should return 200 ok', async () => {
            
        });

        it('should should return JSON', () => {
            
        });

        it(`should should return {message: 'Welcome to the Main API'}`, () => {
            
        });
    });
});